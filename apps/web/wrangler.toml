name = "openreel"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages configuration for AI-Video-Stitching-Pipeline
# This app requires special headers for SharedArrayBuffer (used by FFmpeg.wasm)
# Custom domain: configure before production deployment

[env.production]
name = "openreel"

[env.preview]
name = "openreel-preview"
